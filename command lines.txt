command palette -> select interpreter -> select anaconda3 -> e.g Python 3.10.0
python3 -m pip install torch torchvision if it says torch is not installed.
pip install llama-cpp-python==0.1.83
conda create -n localGPT python=3.10.0
conda activate localGPT
pip install torch
pip install langchain=0.0.267
pip install chromadb==0.4.6
conda install -r requirements.txt

conda install pip
conda activate then run the command
python -m pip install -r requirements.txt

pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu117

If errors in building llama-cpp-python==0.1.83
download cmake tools in visual studio 2022
then download llama-cpp-python==0.1.83
pip install --force-reinstall charset-normalizer==
3.1.0
